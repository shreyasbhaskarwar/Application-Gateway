language: node_js
node_js:
  - lts/*
install:
  - yarn install
script:
  - yarn docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: ${{ secrets.ACCESS_TOKEN }}
  keep_history: true
  on:
    push:
      branch: create-website-for-project
